// src/db/db.service.ts
// Aquí iría la inicialización de tu DB (ej. initializeApp de Firebase)
// Por ahora (KISS), actúa como un placeholder limpio.
export const dbConnection = {
  isConnected: true,
  log: () => console.log('DB Conectada exitosamente'),
};